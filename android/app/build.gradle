// Android build configuration for Mind Wars app
// Note: Launcher icons (ic_launcher.png) in res/mipmap-* directories must be:
//   - Valid, non-corrupted PNG files to avoid AAPT2 compilation errors
//   - Properly sized for each density (mdpi:48px, hdpi:72px, xhdpi:96px, xxhdpi:144px, xxxhdpi:192px)
//   - Stripped of problematic metadata using tools like ImageMagick or optipng
// See android/app/src/main/res/README.md for more details on icon requirements

plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

def localProperties = new Properties()
def localPropertiesFile = rootProject.file('local.properties')
if (localPropertiesFile.exists()) {
    localPropertiesFile.withReader('UTF-8') { reader ->
        localProperties.load(reader)
    }
}

def flutterVersionCode = localProperties.getProperty('flutter.versionCode')
if (flutterVersionCode == null) {
    flutterVersionCode = '1'
}

def flutterVersionName = localProperties.getProperty('flutter.versionName')
if (flutterVersionName == null) {
    flutterVersionName = '1.0'
}

android {
    namespace "com.mindwars.app"
    compileSdk 34
    ndkVersion flutter.ndkVersion

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = '1.8'
    }

    sourceSets {
        main.java.srcDirs += 'src/main/kotlin'
    }

    defaultConfig {
        applicationId "com.mindwars.app"
        // Minimum SDK: Android 8.0 (API 26)
        minSdk 26
        targetSdk 33
        versionCode flutterVersionCode.toInteger()
        versionName flutterVersionName
        
        // MultiDex support for large apps
        multiDexEnabled true
        
        // Vector drawables support
        vectorDrawables.useSupportLibrary = true
    }

    // Product flavors for different build variants
    flavorDimensions "version"
    productFlavors {
        production {
            dimension "version"
            applicationIdSuffix ""
            versionNameSuffix ""
        }
        
        alpha {
            dimension "version"
            applicationIdSuffix ".alpha"
            versionNameSuffix "-alpha"
        }
    }

    buildTypes {
        release {
            // Signing config for release builds
            signingConfig signingConfigs.debug
            
            // Code shrinking and obfuscation
            // Note: Ensure all resources (especially PNG files) are valid before enabling
            // shrinkResources, as AAPT2 will fail on corrupted image files during compilation
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
        
        debug {
            applicationIdSuffix ".debug"
            debuggable true
        }
    }
    
    // Split APKs by ABI for smaller download sizes
    splits {
        abi {
            enable true
            reset()
            include 'armeabi-v7a', 'arm64-v8a', 'x86_64'
            universalApk true
        }
    }
    
    // Optimize for different screen densities
    packagingOptions {
        exclude 'META-INF/DEPENDENCIES'
        exclude 'META-INF/LICENSE'
        exclude 'META-INF/LICENSE.txt'
        exclude 'META-INF/license.txt'
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/NOTICE.txt'
        exclude 'META-INF/notice.txt'
        exclude 'META-INF/ASL2.0'
    }
}

flutter {
    source '../..'
}

dependencies {
    // Material Design 3 support
    implementation 'com.google.android.material:material:1.10.0'
    
    // MultiDex support
    implementation 'androidx.multidex:multidex:2.0.1'
}
